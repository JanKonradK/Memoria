{
  "name": "@technogg/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute technogg --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute technogg --local --file=./schema.sql"
  },
  "dependencies": {
    "@technogg/shared": "*",
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.16.1"
  }
}
